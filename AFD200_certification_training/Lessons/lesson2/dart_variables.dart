// dart vars are similar to what I already know. They are a piece of memory
// that contain a data value. dart vars are case sensitive.

main() {
  print('Welcome to Android ATC');
  var x = 1;
  print(x);
  print(y);
}

// we an also do vars outside of main and reference
var y = 2;
